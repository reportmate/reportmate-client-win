name: ReportMatePrefs
version: "1.0.0"
description: "ReportMate Client Preferences Configuration Package"
maintainer: "ReportMate Team"
architecture: "amd64"
platform: "windows"

# Package metadata
metadata:
  category: "System Administration"
  tags: 
    - "reportmate"
    - "configuration"
    - "enterprise"
    - "client"
  homepage: "https://github.com/reportmate/reportmate-client-win"
  documentation: "https://github.com/reportmate/reportmate-client-win/blob/main/README.md"

# Dependencies
dependencies:
  - name: "PowerShell"
    version: ">=5.0"
    description: "Windows PowerShell 5.0 or later required for script execution"

# Package contents
payload:

# Installation scripts
scripts:
  postinstall: "postinstall.ps1"
  preremove: "preremove.ps1"

# Package configuration
config:
  install_location: "C:\\Program Files\\Cimian"
  requires_admin: true
  restart_required: false
